import React from 'react';

const List = props => (
    <ul>
        {props.items.map((items, index) => <li key={index}>{item}  <h1>{this.state.temp}°C</h1>
            <h1>{this.state.des}</h1></li>)}
    </ul>
);

export default List;